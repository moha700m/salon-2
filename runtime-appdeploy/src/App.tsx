import { useEffect, useMemo, useState } from 'react';
import { InteractiveServices } from './InteractiveServices';
import { api } from '@appdeploy/client';
import { MapPin, Sparkles, Star, Phone, Clock3, ExternalLink, Copy, Check, WandSparkles, CalendarDays, ArrowLeft, MessageCircle, Scissors, ShieldCheck } from 'lucide-react';

type Site={id:string;name:string;category:string;address:string;phone:string;website:string;rating:number;reviewCount:number;hours:string[];description:string;services:string[];reviews:string[];mapsUrl:string;createdAt:string;missing:string[]};
type GenerateResponse={site:Site};

const digits=(v:string)=>v.replace(/\D/g,'');
const wa=(p:string)=>{let d=digits(p);if(d.startsWith('05'))d='966'+d.slice(1);return d?`https://wa.me/${d}`:'#'};
const share=(id:string)=>`${window.location.origin}${window.location.pathname}?site=${encodeURIComponent(id)}`;

function SalonSite({site,onBack}:{site:Site;onBack?:()=>void}){const [form,setForm]=useState({name:'',phone:'',service:site.services[0]||'خدمة تجميل',date:'',time:''});const [done,setDone]=useState('');const [busy,setBusy]=useState(false);const minDate=new Date().toISOString().slice(0,10);const book=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setDone('');try{const r=await api.post('/api/bookings',{siteId:site.id,...form});setDone(`تم إرسال الحجز بنجاح — ${r.data.booking.ref}`);setForm({...form,name:'',phone:''})}catch(err){const m=err as {message?:string};setDone(m.message||'تعذر إرسال الحجز، جرّبي موعدًا آخر.')}finally{setBusy(false)}};return <div className="salon-site"><header className="salon-nav"><button className="brand"><span className="brand-orb">{site.name.slice(0,1)}</span><span>{site.name}</span></button><nav><a href="#services">الخدمات</a><a href="#reviews">التقييمات</a><a href="#booking">الحجز</a></nav>{onBack&&<button className="ghost" onClick={onBack}><ArrowLeft size={17}/>المنصة</button>}</header><main><section className="hero"><div className="hero-copy"><div className="eyebrow"><Sparkles size={16}/> تجربة جمال مصممة لك</div><h1>{site.name}</h1><p>{site.description||`وجهتك للعناية والجمال في ${site.address||'مدينتك'}. احجزي موعدك بسهولة واكتشفي خدماتنا.`}</p><div className="hero-meta"><span><Star size={16} fill="currentColor"/>{site.rating||'—'} <small>({site.reviewCount||0} تقييم)</small></span><span><MapPin size={16}/>{site.address||'العنوان متاح عبر Google Maps'}</span></div><div className="hero-actions"><a className="primary" href="#booking">احجزي موعدك</a><a className="secondary" href={site.mapsUrl} target="_blank" rel="noreferrer"><MapPin size={18}/>الموقع</a>{site.phone&&<a className="secondary" href={wa(site.phone)} target="_blank" rel="noreferrer"><MessageCircle size={18}/>واتساب</a>}</div></div><div className="hero-art"><div className="photo-card photo-a"><span>صور المشغل</span></div><div className="photo-card photo-b"><Scissors size={34}/><strong>{site.category||'Beauty Salon'}</strong></div><div className="rating-card"><span className="stars">★★★★★</span><b>{site.rating||'جديد'}</b><small>على Google</small></div></div></section><section className="trust-strip"><div><ShieldCheck/><b>حجز مباشر</b><span>بدون اتصالات وانتظار</span></div><div><Clock3/><b>ساعات واضحة</b><span>{site.hours[0]||'تواصلي لمعرفة ساعات العمل'}</span></div><div><Star/><b>تجربة موثوقة</b><span>{site.reviewCount?`${site.reviewCount} مراجعة على Google`:'تقييمات العملاء في مكان واحد'}</span></div></section><section id="services" className="section interactive-services-section"><div className="section-head"><div><span>خدماتنا</span><h2>اختاري خدمتك من الدائرة</h2></div><p>اضغطي على أي دائرة لتظهر لكِ تفاصيل الخدمة، ثم احجزيها مباشرة عبر واتساب.</p></div><InteractiveServices services={site.services} phone={site.phone} businessName={site.name}/></section><section id="reviews" className="section soft"><div className="section-head"><div><span>آراء العميلات</span><h2>سمعة تُرى قبل الزيارة</h2></div><div className="google-score"><Star fill="currentColor"/>{site.rating||'—'} / 5</div></div><div className="review-grid">{(site.reviews.length?site.reviews:['تجربة راقية واهتمام بالتفاصيل.','الموقع مرتب والحجز سهل وواضح.','خدمة مميزة وتعامل احترافي.']).slice(0,3).map((r,i)=><blockquote key={i}><div className="stars">★★★★★</div><p>“{r}”</p><footer>عميلة عبر Google</footer></blockquote>)}</div></section><section className="section split"><div><span className="mini">زورينا</span><h2>الموقع وساعات العمل</h2><p className="address"><MapPin/>{site.address||'افتحي Google Maps للوصول للموقع'}</p><a className="primary inline" href={site.mapsUrl} target="_blank" rel="noreferrer">فتح Google Maps <ExternalLink size={16}/></a></div><div className="hours">{site.hours.length?site.hours.map((h,i)=><div key={i}><Clock3 size={17}/><span>{h}</span></div>):<div><Clock3 size={17}/><span>ساعات العمل غير ظاهرة في المصدر — تواصلي مع المشغل</span></div>}</div></section><section id="booking" className="booking"><div className="booking-copy"><span>احجزي الآن</span><h2>موعدك القادم يبدأ من هنا</h2><p>اختاري الخدمة والموعد المناسب وأرسلي الطلب خلال أقل من دقيقة.</p><div className="booking-note"><CalendarDays/><span>الحجز يُحفظ مباشرة باسم المشغل ويمنع حجز نفس الموعد مرتين.</span></div></div><form onSubmit={book}><label>الاسم<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="اسم العميلة"/></label><label>رقم الجوال<input required pattern="05[0-9]{8}" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="05xxxxxxxx"/></label><label>الخدمة<select value={form.service} onChange={e=>setForm({...form,service:e.target.value})}>{site.services.map(s=><option key={s}>{s}</option>)}</select></label><div className="row"><label>التاريخ<input required type="date" min={minDate} value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>الوقت<input required type="time" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/></label></div><button className="primary submit" disabled={busy}>{busy?'جارٍ إرسال الحجز...':'تأكيد طلب الحجز'}</button>{done&&<div className="form-status">{done}</div>}</form></section></main><footer className="salon-footer"><div><strong>{site.name}</strong><span>{site.category}</span></div><div className="footer-links">{site.phone&&<a href={`tel:${site.phone}`}><Phone size={15}/>{site.phone}</a>}<a href={site.mapsUrl} target="_blank" rel="noreferrer"><MapPin size={15}/>Google Maps</a>{site.website&&<a href={site.website} target="_blank" rel="noreferrer"><ExternalLink size={15}/>الموقع</a>}</div></footer></div>}

function App(){const params=useMemo(()=>new URLSearchParams(window.location.search),[]);const initialId=params.get('site');const [site,setSite]=useState<Site|null>(null);const [mapsUrl,setMapsUrl]=useState('');const [busy,setBusy]=useState(false);const [stage,setStage]=useState('');const [error,setError]=useState('');const [copied,setCopied]=useState(false);useEffect(()=>{if(initialId){setBusy(true);api.get(`/api/site/${encodeURIComponent(initialId)}`).then(r=>setSite(r.data.site)).catch(()=>setError('تعذر العثور على هذا الموقع')).finally(()=>setBusy(false))}},[initialId]);const generate=async(demo=false)=>{setBusy(true);setError('');setStage(demo?'نبني نموذجًا تجريبيًا...':'نقرأ بيانات Google Maps...');try{if(!demo&&!mapsUrl.trim()){setError('الصق رابط Google Maps أولًا.');return}setTimeout(()=>setStage('نرتب الاسم والتقييم والخدمات وساعات العمل...'),700);const r=await api.post('/api/generate',{mapsUrl:mapsUrl.trim(),demoMode:demo}) as {data:GenerateResponse};setStage('نركّب الموقع على قالب المشاغل...');setSite(r.data.site);window.history.replaceState({},'',`?site=${encodeURIComponent(r.data.site.id)}`)}catch(err){const e=err as {message?:string};setError(e.message||'تعذر قراءة الرابط. تأكد أنه رابط Google Maps عام وحاول مرة أخرى.')}finally{setBusy(false);setStage('')}};const copy=async()=>{if(!site)return;await navigator.clipboard.writeText(share(site.id));setCopied(true);setTimeout(()=>setCopied(false),1600)};if(initialId&&busy&&!site)return <div className="loading-page"><WandSparkles/><b>جارٍ فتح موقع المشغل...</b></div>;if(site)return <><div className="preview-bar"><div><span>معاينة جاهزة</span><b>{site.name}</b></div><div>{site.missing.length>0&&<span className="missing">ناقص: {site.missing.join('، ')}</span>}<button className="ghost" onClick={copy}>{copied?<Check size={17}/>:<Copy size={17}/>} {copied?'تم النسخ':'نسخ رابط المعاينة'}</button><button className="dark" onClick={()=>{window.history.replaceState({},'',window.location.pathname);setSite(null);setMapsUrl('')}}>مشغل جديد</button></div></div><SalonSite site={site} onBack={()=>{window.history.replaceState({},'',window.location.pathname);setSite(null)}}/></>;return <div className="builder"><header className="builder-nav"><div className="builder-logo"><span><WandSparkles/></span><div><b>مشاغل</b><small>Salon Site Factory</small></div></div><div className="builder-badge">قالب بنان · إصدار 01</div></header><main className="builder-main"><div className="builder-copy"><div className="eyebrow"><Sparkles size={16}/> من رابط إلى موقع كامل</div><h1>الصق رابط Google Maps.<br/><em>والباقي علينا.</em></h1><p>نقرأ بيانات المشغل، نبني الهوية والمحتوى، ونطلع لك رابط معاينة جاهز ترسله للعميل خلال دقائق.</p><div className="steps"><div><span>1</span><p><b>استخراج البيانات</b><small>الاسم، الهاتف، العنوان، التقييم، الساعات والموقع</small></p></div><div><span>2</span><p><b>تركيب القالب</b><small>خدمات، تقييمات، خرائط، واتساب وحجز</small></p></div><div><span>3</span><p><b>رابط جاهز للبيع</b><small>معاينة مستقلة لكل مشغل وإظهار النواقص فقط</small></p></div></div></div><div className="generator-card"><div className="card-top"><span className="live-dot"/> مولّد المواقع الذكي</div><label>رابط Google Maps</label><div className="url-box"><MapPin/><input value={mapsUrl} onChange={e=>setMapsUrl(e.target.value)} placeholder="https://maps.app.goo.gl/..."/></div><button className="generate" onClick={()=>generate(false)} disabled={busy}><WandSparkles/>{busy?(stage||'جارٍ الإنشاء...'):'أنشئ موقع المشغل'}</button>{error&&<div className="error-box">{error}</div>}<div className="or"><span/>أو<span/></div><button className="demo" onClick={()=>generate(true)} disabled={busy}>جرّب مثالًا جاهزًا</button><div className="privacy"><ShieldCheck size={15}/>لا نطلب تسجيل دخول المشغل؛ نستخدم البيانات العامة الظاهرة فقط.</div></div></main><section className="builder-preview"><div><span>القالب المحفوظ</span><h2>نفس قوة بنان، لأي مشغل.</h2></div><div className="mini-browser"><div className="browser-top"><i/><i/><i/><span>salon-preview</span></div><div className="browser-body"><div className="mock-nav"/><div className="mock-hero"><div><b/><b/><p/><p/><button/></div><div className="mock-photo"/></div><div className="mock-cards"><i/><i/><i/></div></div></div></section></div>}

export default App;